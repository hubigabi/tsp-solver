import {CostMatrixValidPipe} from "./cost-matrix-valid.pipe";

describe('AlgorithmFormVisiblePipe', () => {
  it('create an instance', () => {
    const pipe = new CostMatrixValidPipe();
    expect(pipe).toBeTruthy();
  });
});
