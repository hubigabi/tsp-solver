import { RouteCostPipe } from './route-cost.pipe';

describe('RouteCostPipe', () => {
  it('create an instance', () => {
    const pipe = new RouteCostPipe();
    expect(pipe).toBeTruthy();
  });
});
