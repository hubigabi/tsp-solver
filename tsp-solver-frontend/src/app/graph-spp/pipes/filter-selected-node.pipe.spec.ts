import { FilterSelectedNodePipe } from './filter-selected-node.pipe';

describe('FilterSelectedNodePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterSelectedNodePipe();
    expect(pipe).toBeTruthy();
  });
});
